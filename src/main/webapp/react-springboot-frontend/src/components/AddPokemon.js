import React, {Component} from "react";

export default class AddPokemon extends Component{
    render(){
        return(
            <div className="text-white">Add Pokemon Page</div>
        );
    }
}